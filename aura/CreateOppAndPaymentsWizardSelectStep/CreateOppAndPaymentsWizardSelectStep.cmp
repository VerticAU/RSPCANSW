<aura:component extends="c:vertic_Step">

    <!-- ATTRIBUTE OVERRIDES -->
    <aura:set attribute="name" value="select"/>
    <aura:set attribute="showCancelButton" value="{!false}"/>
    <aura:set attribute="quickActionHeightPX" value="600"/>
    <aura:set attribute="quickActionMaxWidthRem" value="70"/>

    <aura:attribute name="columns" type="Map"/>
    <aura:attribute name="recordId" type="String"/>

    <aura:handler name="init" value="{!this}" action="{!c.handleInit}"/>
    <aura:handler name="change" value="{!v.meta.dto.pricebookId}" action="{!c.handlePriceBookChange}"/>

    <aura:set attribute="content">
        <h2 class="slds-text-heading--small slds-text-align--center slds-m-bottom--x-small">Create Opportunity &amp; Payments</h2>
        <div class="slds-m-around_medium">
            <c:strike_lookup_clickable label="Price Book"
                                       object="Pricebook2"
                                       searchField="Name"
                                       placeholder="Select a Price Book..."
                                       iconName="standard:account"
                                       order="Name"
                                       limit="8"
                                       loadingMessage="Loading..."
                                       errorMessage="Invalid input"
                                       showRecentRecords="{!true}"
                                       disabled="{!v.isBusy}"
                                       value="{!v.meta.dto.pricebookId}"
            />
        </div>

        <aura:if isTrue="{! not(empty(v.meta.dto.productItems)) }">
            <div class="slds-m-around_medium">
                <div class="slds-box--border">
                    <lightning:datatable aura:id="productTable"
                                         keyField="id"
                                         data="{! v.meta.dto.productItems }"
                                         columns="{! v.columns }"
                                         onrowaction="{! c.handleRowAction }"
                                         onsave="{!c.handleSave}"
                                         hideCheckboxColumn="true"/>
                </div>
            </div>
            <aura:set attribute="else">
                <div class="slds-box--border slds-m-around_medium">
                    <div class="slds-p-around--x-small slds-text-align--center">
                        No Records
                    </div>
                </div>
            </aura:set>
        </aura:if>
    </aura:set>

    <aura:set attribute="customButtons">
        <div class="slds-grid">
            <div class="slds-col slds-text-align_left slds-size_1-of-5">
                {!v.cancelButton}
            </div>
            <div class="slds-col slds-size_3-of-5">
                <c:CreateOppAndPaymentsWizardNavigation currentStep="{!v.name}"/>
            </div>
            <div class="slds-col slds-text-align_right slds-size_1-of-5">
                {!v.nextStepButton}
            </div>
        </div>
    </aura:set>
</aura:component>